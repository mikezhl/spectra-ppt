# Spectra Deck 关键约束

## 不要再犯的布局错误

- 这是固定宽度长网页，不是响应式网站：设计宽度固定为 `1440px`，小屏只能整体缩放，不能改列数、换布局、重排卡片。
- 这是网页式连续叙事，不是 PPT：section 高度由内容决定，不能把每个 section 强行做成固定一屏一页。
- 固定宽度和连续网页必须同时满足：外层负责缩放，内部永远按同一套 1440px 桌面布局渲染。


## 视觉方向

- 微软蓝只做克制强调，整体保持白色、浅灰、蓝色的极简扁平风格。
- 不要为了“网页感”改成移动端自适应，也不要为了“deck 感”退回一页一屏。
